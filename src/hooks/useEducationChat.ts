
import { useState } from 'react';

export const useEducationChat = () => {
  // Placeholder implementation
  return {
    messages: [],
    isLoading: false,
    error: null,
    sendMessage: async () => {},
    clearMessages: () => {}
  };
};

export default useEducationChat;
